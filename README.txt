STG623/stem-to-grow — ready for GitHub Pages

- Cursor shows ONLY on clickable elements (links, buttons, nav, CTA).
- Put your real cursor image as: images/mr-squishy-cursor.png (keep same name).
- Files live in the repo root, so relative paths like images/... work at
  https://stg623.github.io/stem-to-grow/

Deploy:
1) Upload everything to the repo root.
2) Settings → Pages → Deploy from branch → main → root.
3) Hard refresh after publish (Ctrl+F5 / Cmd+Shift+R).
